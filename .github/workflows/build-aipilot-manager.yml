name: Build AIPilot Manager

on:
  workflow_dispatch:
  push:
    paths:
      - "manager-app/**"
      - ".github/workflows/build-aipilot-manager.yml"

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.os == 'ubuntu-latest' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            script: dist:win
            args: ""
            artifact: aipilot-manager-windows
          - os: ubuntu-latest
            script: dist:linux
            args: ""
            artifact: aipilot-manager-linux
          - os: macos-latest
            script: dist:mac
            args: "-- --x64"
            artifact: aipilot-manager-macos-x64
          - os: macos-latest
            script: dist:mac
            args: "-- --arm64"
            artifact: aipilot-manager-macos-arm64

    defaults:
      run:
        working-directory: manager-app

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: manager-app/package-lock.json

      - name: Install manager dependencies
        run: npm ci

      - name: Install Linux packaging dependencies
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends rpm fakeroot dpkg libarchive-tools

      - name: Build packaged artifacts
        run: npm run ${{ matrix.script }} ${{ matrix.args }}

      - name: Upload manager artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            manager-app/dist/*.exe
            manager-app/dist/*.blockmap
            manager-app/dist/*.yml
            manager-app/dist/*.AppImage
            manager-app/dist/*.deb
            manager-app/dist/*.dmg
            manager-app/dist/*.zip
          if-no-files-found: error
